# První úkol

Otevři ukázkový projekt, nech Claude udělat jednu reálnou změnu, koukni, co se stane — na tvém stacku.

Dole máš dvě paralelní varianty — vyber si tu, která sedí k tvému dennímu stacku, a druhou přeskoč. Obě nepotřebuješ.

- **Varianta A — .NET Core** · ASP.NET Core minimal API. Pro C# / .NET vývojáře.
- **Varianta B — Python + React** · FastAPI backend, React frontend. Pro Python / JS vývojáře.

Naklonuj repo jednou a pak skoč do své varianty:

```bash
git clone https://github.com/ondrej-svec/claude-code-lab.git
cd claude-code-lab
```

## Varianta A — .NET Core

Otevři `samples/dotnet-core/` v Claudu. Cíl: přidat `GET /health` endpoint do minimal API. Jedna konkrétní změna, celou smyčkou.

### 1. Požádej o změnu

V Claude okně:

> Přečti `Program.cs`, ať víš tvar, a pak přidej `GET /health` endpoint, který vrací `{ "ok": true, "uptime_seconds": n }`.

<Screenshot
  src="/screenshots/ch2-plan-output.png"
  alt="Plán Claude Code pro .NET /health endpoint — soubor k editaci (Program.cs) a dotnet test kontrola"
  caption="Claude plánuje první. Přečti si to, než zmáčkneš y."
/>

**Co se právě stalo.** Claude zatím nic needitoval. Přečetl `Program.cs`, načrtl plán a pojmenoval přesně soubor i ověřovací krok. Žádný kód se nehnul. Tvar schvaluješ dřív, než přistane jediný znak.

### 2. Potvrď a koukni na diff

Řekni ano. Claude změnu zapíše a ukáže jako diff, než ji aplikuje.

<Screenshot
  src="/screenshots/ch2a-diff.png"
  alt="Diff Program.cs s přidaným using System.Diagnostics, Stopwatch startedAt a MapGet /health routou"
  caption="Malý, soustředěný diff. Dvě přidání, žádné mazání."
/>

Tři řádky reálného kódu (using, startedAt, MapGet) a dvě prázdné. Nic jiného se nehnulo. Takhle „malý diff“ vypadá v praxi — pokud první pokus rozlezl, zúžni prompt a zkus znova.

### 3. Spusť to

Potvrď. Pak spusť sample:

> Spusť aplikaci a zavolej `/health`, ať ověříš, že to sedí.

<Screenshot
  src="/screenshots/ch2a-run.png"
  alt="Terminál s dotnet run a curl localhost:5055/health vrací 200 OK s ok true a uptime_seconds"
  caption="200 OK. Endpoint žije. Smyčka uzavřená."
/>

Máš běžící feature, kterou můžeš commitnout.

**Když se něco pokazí**

- Build spadne? Vlož chybu zpátky: „build selhal: `[paste]` — oprav to.“ Claude vidí svoji vlastní změnu v kontextu a obvykle to opraví jedním krokem.
- Curl vrací 500? Vlož server log. Nehádej; ukaž Claudovi, co rozbil.

## Varianta B — Python + React

Otevři `samples/python-react/` v Claudu. Cíl: totéž — přidat `GET /health` endpoint do FastAPI backendu. Jedna konkrétní změna, celou smyčkou.

### 1. Požádej o změnu

V Claude okně:

> Přečti `backend/main.py`, ať víš tvar, a pak přidej `GET /health` endpoint, který vrací `{ "ok": true, "uptime_seconds": n }`.

<Screenshot
  src="/screenshots/ch2b-plan-output.png"
  alt="Plán Claude Code pro FastAPI /health endpoint — soubor k editaci (backend/main.py) a curl kontrola"
  caption="Claude plánuje první. Přečti si to, než zmáčkneš y."
/>

**Co se právě stalo.** Claude se zatím žádného souboru nedotkl. Otevřel `backend/main.py`, načrtl plán a pojmenoval ověřovací krok, který chce spustit po úpravě. Plán je levný — můžeš ho odmítnout, přeformulovat, nebo potvrdit. Editace začíná až po `y`.

### 2. Potvrď a koukni na diff

Řekni ano. Claude změnu zapíše a ukáže jako diff.

<Screenshot
  src="/screenshots/ch2b-diff.png"
  alt="Diff backend/main.py s přidaným importem time, _started_at časovačem a /health routou"
  caption="Malý, soustředěný diff. Bez zásahů jinam."
/>

Jeden import, jeden modulový časovač, jeden route. Tečka. Existující middleware a routy jsou nedotčené — dobrá agentní práce se nerozlézá.

### 3. Spusť to

Potvrď. Pak pusť backend:

> Pusť uvicorn a zavolej `/health`, ať ověříš, že to sedí.

<Screenshot
  src="/screenshots/ch2b-run.png"
  alt="Terminál s běžícím uvicornem na portu 8000 a curl, který vrací 200 OK s ok true a uptime_seconds"
  caption="200 OK. Endpoint žije. Smyčka uzavřená."
/>

Máš běžící feature, kterou můžeš commitnout.

**Bonus (když chceš frontend beat):** požádej Claude, ať ve React aplikaci udělá malou „Backend: OK“ indikaci, která při mountu volá `/health`. Jeden komponent, jedno fetch volání. Stejná smyčka, o patro výš.

**Když se něco pokazí**

- Uvicorn spadne? Vlož traceback: „uvicorn spadl: `[paste]` — oprav to.“
- Curl vrací 500? Ukaž server log. Nehádej; ukaž Claudovi, co rozbil.

## Co jsi právě procítil

Obě varianty šly stejným tvarem: **popiš → naplánuj → diff → spusť**. Tenhle tvar je páteří každé agentní session. Zbytek labu si své místo zaslouží tím, že každý z těch čtyř kroků postupně dělá líp.

Pár signálů, které stojí za pojmenování teď, ať je později poznáš:

- **Plán ti ušetřil jednu iteraci.** Přečíst si plán, než zmáčkneš `y`, je levnější než kontrolovat špatný diff. Pozdější kapitoly posouvají plány dál — zapsané na disk, řízené dalšími příkazy, zkontrolované dřív, než se kód hne. Smyčka Compound Engineeringu v [kapitole 6](/cs/lab/compound-engineering) je místo, kde to plně sedne.
- **Diff šel zkontrolovat.** Tři až pět řádků, ne třicet. [Kapitola 4](/cs/lab/iteration-and-control) mluví o tom, jak držet diffy malé záměrně — plan mode, rewind, zúžený scope.
- **Agentovi jsi nevěřil — ověřil jsi.** Spuštění endpointu bylo skutečné schválení, ne stisk `y` na diffu. [Kapitola 7](/cs/lab/next-steps) jde do hloubky na to, jak tohle ověřování dělat automatičtěji (testy, scany, CI review).
- **Agent pracoval na tvém stacku, ne na hračce.** Tvoje vlastní projekty mají kontext, který agent bude potřebovat. [Kapitola 3](/cs/lab/teach-claude-your-project) je o tom, jak mu ten kontext předat čistě.

Čtyři momenty. Jedna malá feature. Udělej totéž na skutečném repu zítra — stejný tvar, stejný rytmus.

Každá další kapitola končí blokem **Praxe: pokračuj se svou ukázkou** — tvoje ukázka roste s tebou, nebo vezmeš stejné pohyby rovnou na svoje vlastní repo.

Tahle smyčka — popiš, naplánuj, diff, zkontroluj, iteruj — je tvar, který Anthropic doporučuje na všechno. Jejich [Claude Code best practices](https://www.anthropic.com/engineering/claude-code-best-practices) stojí za přečtení, až to procítíš.
