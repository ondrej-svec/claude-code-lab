version: 2
updates:
  - package-ecosystem: npm
    directory: /
    schedule:
      interval: weekly
      day: monday
    open-pull-requests-limit: 5
    ignore:
      # eslint 10 removed context.getFilename(); eslint-config-next still pulls
      # eslint-plugin-react@^7.37.x (peer-capped at eslint ^9.7), which has no
      # ESLint 10-compatible release. Lint fails hard on the major bump. Remove
      # this rule once eslint-plugin-react ships ESLint 10 support.
      - dependency-name: "eslint"
        update-types: ["version-update:semver-major"]
      # typescript-eslint does not support TS 7.0 yet (throws at load: "does
      # not support TS 7.0"), so the major bump fails `pnpm lint` hard. Tracking:
      # https://github.com/typescript-eslint/typescript-eslint/issues/10940
      # Remove this rule once typescript-eslint ships TS >=7 support.
      - dependency-name: "typescript"
        update-types: ["version-update:semver-major"]
    groups:
      next-ecosystem:
        patterns:
          - "next"
          - "@next/*"
          - "eslint-config-next"
      tailwind-ecosystem:
        patterns:
          - "tailwindcss"
          - "@tailwindcss/*"
      react-ecosystem:
        patterns:
          - "react"
          - "react-dom"
          - "@types/react*"
      dev-dependencies:
        dependency-type: development

  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
      day: monday
    open-pull-requests-limit: 3
